# ACL configuration to allow service account to search users
# This modifies the default ACLs to grant read access to ldapbind service account

dn: olcDatabase={1}mdb,cn=config
changetype: modify
replace: olcAccess
olcAccess: {0}to attrs=userPassword,shadowLastChange
  by self write
  by anonymous auth
  by dn="cn=admin,dc=test,dc=local" write
  by dn="cn=ldapbind,ou=services,dc=test,dc=local" read
  by * none
olcAccess: {1}to dn.base=""
  by * read
olcAccess: {2}to *
  by self write
  by dn="cn=admin,dc=test,dc=local" write
  by dn="cn=ldapbind,ou=services,dc=test,dc=local" read
  by users read
  by * none
